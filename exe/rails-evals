#!/usr/bin/env ruby
# frozen_string_literal: true

# Load-path require, as RubyGems and Bundler arrange: run from a checkout with
# `ruby -Ilib exe/rails-evals` or `bundle exec`.
require "agent_harness_rails/evals/cli"

exit AgentHarnessRails::Evals::CLI.new(ARGV).run
