#!/usr/bin/env ruby
# frozen_string_literal: true

# Load-path require, as RubyGems and Bundler arrange: run from a checkout with
# `ruby -Ilib exe/agent_harness_rails` or `bundle exec`.
require "agent_harness_rails/cli"

exit AgentHarnessRails::CLI.new(ARGV).run
